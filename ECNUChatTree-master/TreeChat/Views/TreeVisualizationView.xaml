<UserControl x:Class="TreeChat.Views.TreeVisualizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d" 
             d:DesignHeight="4500" d:DesignWidth="8000">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- 搜索栏 -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="5" Background="#F0F0F0">
            <TextBlock Text="搜索:" VerticalAlignment="Center" Margin="5,0"/>
            <TextBox Width="150" Text="{Binding SearchKeyword, UpdateSourceTrigger=PropertyChanged}"
                     KeyDown="SearchBox_KeyDown" Margin="5,0" Padding="3"/>
            <Button Content="搜索" Command="{Binding SearchCommand}" Width="50" Margin="2,0"/>
            <Button Content="◀" Command="{Binding NavigatePreviousCommand}" Width="25" Margin="2,0" ToolTip="上一个"/>
            <Button Content="▶" Command="{Binding NavigateNextCommand}" Width="25" Margin="2,0" ToolTip="下一个"/>
            <TextBlock Text="{Binding MatchInfo}" VerticalAlignment="Center" Margin="5,0" Foreground="Gray"/>
            <Button Content="删除节点" Command="{Binding DeleteNodeCommand}" Width="70" Margin="10,0" ToolTip="删除选中的节点"/>
        </StackPanel>

        <!-- 树画布 -->
        <ScrollViewer Grid.Row="1" x:Name="scrollViewer"
                      Background="Transparent"
                      HorizontalScrollBarVisibility="Auto" 
                      VerticalScrollBarVisibility="Auto"
                      PreviewMouseWheel="ScrollViewer_PreviewMouseWheel"
                      PreviewMouseLeftButtonDown="ScrollViewer_PreviewMouseLeftButtonDown"
                      MouseMove="ScrollViewer_MouseMove"
                      MouseLeftButtonUp="ScrollViewer_MouseLeftButtonUp">
            <Canvas x:Name="treeCanvas" Background="Transparent"/>
        </ScrollViewer>
    </Grid>
</UserControl>
